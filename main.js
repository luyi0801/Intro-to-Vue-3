const app = Vue.createApp({
    data() {
        return {
            cart: 0,
            premium: true,
            details:"that is a pair of sock"
        }
    },
    methods: {
    },
    computed: {
    }
})
